import { InvalidEmailDirective } from './invalid-email.directive';

describe('InvalidEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidEmailDirective();
    expect(directive).toBeTruthy();
  });
});
